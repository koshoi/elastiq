[env.dev]
endpoints = ["http://localhost:9200"]
default   = true
index     = "mydata-*"
output    = "pretty"

[output.pretty]
exclude = ["kubernetes", "message", "@timestamp"]
format = "json"
decode_recursively = true

[output.message]
only = ["message"]
format = "json"
